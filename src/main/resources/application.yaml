
spring:
  application:
    name: EduCourseManagementSystem

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5435}/${DB_NAME:learn_db}
    username: ${DB_USERNAME:ferid}
    password: ${DB_PASSWORD:ferid1234}

  jpa:
    hibernate:
      ddl-auto: update


server:
  port: 8081


